[supervisord]
nodaemon=true

[program:nginx]
command=nginx -g 'daemon off;'
autorestart=true
priority=10

[program:php-fpm]
command=php-fpm
autorestart=true
priority=20

[include]
files = /etc/supervisor/conf.d/*.conf
